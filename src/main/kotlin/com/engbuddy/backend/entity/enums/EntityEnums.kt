package com.engbuddy.backend.entity.enums

enum class EnglishLevel { BEGINNER, INTERMEDIATE, ADVANCED }
enum class UserStatus { ACTIVE, INACTIVE, DELETED }
enum class ConversationStatus { ACTIVE, PAUSED, ARCHIVED }
enum class MessageType { TEXT, VOICE, IMAGE, SYSTEM }
enum class SenderType { USER, AI }
